The library will contain:
- BPE module 
- Autograd engine
- Optimizer
- GPT model

The idea is to reimplement every module without using external library, only pure python. 
As follow up work everything should be also implemented in an efficient manner using C++.
